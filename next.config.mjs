/** @type {import('next').NextConfig} */
const nextConfig = {
    compiler: {
        styledComponents: true,
    },
    optimizeCss: false, 
    enableBabelRuntime: true, 
};

export default nextConfig;

